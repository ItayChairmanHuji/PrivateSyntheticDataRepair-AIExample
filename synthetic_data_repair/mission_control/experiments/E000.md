# Experiment E000: Initial Sandbox

## Goal
Scaffold the new "Resources, Processes, Utilities" (RPM) filesystem architecture and migrate the core logic from the legacy ICM stages.

## Status
- [x] Directory Structure created.
- [x] Repository Rules defined.
- [ ] Implement `u_shared` (Entities).
- [ ] Implement `u_io` (Path Resolver).
- [ ] Migrate `p01_loading`.
- [ ] Migrate `p02a_training` and `p02b_sampling`.

## Hypotheses
- A parameter-based filesystem will reduce manual path errors.
- Granular processes (training vs sampling) will speed up hyperparameter sweeps.
- Consolidating configs into a Master Registry will reduce file-system bloat by >90%.
