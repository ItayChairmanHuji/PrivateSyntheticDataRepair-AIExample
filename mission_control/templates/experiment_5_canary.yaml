experiment_group: experiment_5_canary
base_config:
  dataset: adult100
  synthesizer: aim
  epsilon: 0.3
  seed: 42
  repair_algorithm: vanilla_vc
  sample_size: 100
  mode: sample

sweep_parameters:
  synthesizer: [aim, mst]
  repair_algorithm: [vanilla_vc, classic_vc, weighted_vc]
