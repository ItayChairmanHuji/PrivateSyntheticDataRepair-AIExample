experiment_group: experiment_7_canary
base_config:
  dataset: adult100
  synthesizer: aim
  epsilon: 1.0
  seed: 42
  repair_algorithm: vanilla_vc
  sample_size: 1000
  mode: sample
  use_adaptive_alpha: True
  use_auto_alpha: False

sweep_parameters:
  synthesizer: [aim, mst]
  repair_algorithm: [vanilla_vc, classic_vc, weighted_vc]
