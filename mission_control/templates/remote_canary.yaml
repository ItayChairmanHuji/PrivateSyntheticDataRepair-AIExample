experiment_group: remote_canary
base_config:
  dataset: adult
  synthesizer: aim
  epsilon: 0.1
  seed: 42
  repair_algorithm: vanilla_vc
  sample_size: 100
  mode: sample

sweep_parameters:
  repair_algorithm: [vanilla_vc, classic_vc, weighted_vc]
