# 2026-05-23 - Experiment 3 Re-run

## Activities
- Cleaned up all artifacts from previous Experiment 3 runs (blueprints, outputs, remote inputs).
- Updated `mission_control/templates/experiment_3_main.yaml` to match the new definition:
  - `sample_size` set to 5000.
  - Removed `census` dataset from the sweep.
- Generated new blueprints for `experiment_3_canary` and `experiment_3_repair_comparison`.
- Pushed updated codebase to the remote server (`snorlax-login`).
- Successfully executed a canary run on `adult100` (Job ID 46797).
- Deployed the full experiment sweep (720 jobs) to the Slurm cluster (Job ID 46798).
- **Update**: Discovered that the remote `outputs/` folder was polluted with results from a previous run (including `census` data and 50K size samples).
- **Cleanup & Redeploy**:
    - Cancelled Job ID 46798.
    - Deleted polluted remote directories: `outputs/experiment_3_repair_comparison` and `outputs/experiment_3_canary`.
    - Cleaned up polluted local `outputs/` and aggregation files.
    - Redeployed the clean experiment sweep (720 jobs) to the Slurm cluster (Job ID 47518).

## Observations
- The canary run verified that all repair algorithms and synthesizers are working correctly with the current codebase.
- The main experiment is now running on the cluster with 64GB memory and a 4h timeout per job.
