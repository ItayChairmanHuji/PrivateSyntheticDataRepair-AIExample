# 2026-05-25 - Experiment 6 Re-run

## Activities
- **Regenerated Blueprints**: Updated blueprints for `experiment_6_repair_comparison` and `experiment_6_canary` based on the latest templates (modified today at 17:44).
- **Synced Blueprints**: Copied new blueprints to `remote/input/`.
- **Triggered Re-run**: Executed `python -m remote.src.main mode=rerun blueprint=experiment_6_repair_comparison`.
    - Pushed latest code (including `repair_runtime` measurement and fixed alpha selection logic).
    - Cancelled any existing jobs for this blueprint.
    - Deleted remote output directory `outputs/experiment_6_repair_comparison`.
    - Submitted full array of 960 jobs (Job ID 54203).

## Observations
- The Weighted VC algorithm is configured to use `use_auto_alpha: True` (CV-based selection) and `use_adaptive_alpha: False` (no dynamic updates per iteration), as per the latest requirements.
- The `repair_runtime` is now being measured and saved in `metadata.json`.
- The Slurm array is running with the full sweep of 4 datasets, 2 synthesizers, 8 epsilon values, 5 seeds, and 3 repair algorithms.
