# Experiment 5

## Goal
Compare the VC repair algorithms as function of eps. 

## Datasets 
Use adult, census, compas and tax for experiments. 
Use adult100 for canary run. 

## Synthetic data generation 
Create synthetic data with size 50K, generated by models of MST and AIM with different eps. 
The models are already located in the models folder, so sample only. 
Use 5 different seeds for the sampling algorithm. 
The sweep is done here, as you choose different models to sample from. 
You can start the sweep from eps=0.3. 

## Marginals generation 
Select 20 marginals with privacy budget 0.5 for selection and 0.5 for generation. 
- **DCS Attributes**: MUST NOT be excluded (ensure `exclude_dc_attrs: False`).
- **Target Attribute**: MUST be included in every selected marginal (ensure `force_target_attr: True`).

## Repairing
Use the 3 VC repairing algorithms. 
Make sure that: 
1. **Vanilla VC**: Takes the vertex with the maximal degree at each iteration. 
2. **Classic VC**: Selects an edge randomly and removes both endpoints. 
3. **Weighted VC**: MUST use the dynamic (adaptive) alpha calculator. 
4. **Runtime**: The duration of the repair process MUST be measured and saved into `metadata.json` as `repair_runtime`.

## Verification Checklist
- [ ] `s03_marginals` config has `exclude_dc_attrs: False` and `force_target_attr: True`.
- [ ] `s04_repairing` `WeightedVCRepairer` has `use_adaptive_alpha: True`.
- [ ] `s04_repairing` `StageOrchestrator` times the repair call and `ArtifactSaver` saves `repair_runtime`.
- [ ] `s05_evaluating` loads `repair_runtime` and populates `PipelineResult.runtimes`.

## Running on remote
Before running the job on remote, run a canary run on adult100, just to check that all repair algorithms and all generation algorithm work properly. 
When pulling result back, make sure that it is done by zipping the csvs of each experiment and getting it locally. 
Make sure to give the processes enough memory to run and give them few hours timeout. 

## In case of deviation from plan
Stop at any cost and ask me how to proceed. Don't make decisions. 

## Canary run
The canary run should check the entire process on the remote. 
Run it on adult100 with synthetic data of size 100. 
This should be a quick test to verify everything works and nothing fails. 
After it successfully passes, feel free to remove the results of this run. 

## Status (2026-05-26)
- **Fixes Applied**:
    - **Pusher Efficiency**: Exclusion logic in `pusher.py` fixed to skip large folders (`data`, `outputs`, etc.).
    - **Subset Model Resolution**: `runner.py` now strips numeric suffixes from dataset names (e.g., `adult100` -> `adult`) to find base models.
- **Current State**:
    - Canary run on `adult100` PASSED.
    - Full sweep (960 jobs) for `experiment_5_repair_comparison` DEPLOYED (Job ID: 52314).
    - Results pulled locally (903/960 jobs aggregated).
    - Stage 06 Analysis completed: `s06_analysis/notebooks/experiment_5_analysis.ipynb` generated.
